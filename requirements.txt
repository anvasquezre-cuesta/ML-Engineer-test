# Baseline dependencies — change/pin these as you see fit (justify in DECISIONS.md).
# Keep them installable on Python 3.12.

fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings

# OCR / PDF
pytesseract
pymupdf
pillow

# NER
spacy
# after install: python -m spacy download en_core_web_sm

# Fuzzy matching
thefuzz
python-Levenshtein

# Embeddings + vector store
sentence-transformers
qdrant-client

# LLM / HTTP
httpx

# Tests
pytest
pytest-asyncio
